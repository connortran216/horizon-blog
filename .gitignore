# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
storybook-static
dist-ssr
*.local

# Yarn Berry / PnP local artifacts
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Environment variables
.env
.env.local
.env.development.local
.env.production.local
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.swift-module-cache/


.codex/

# oh-my-claudecode session runtime state.
# Rewritten on every turn, and it carries session ids plus project-memory.json's
# absolute path to whoever's machine last scanned - none of which survives a
# clone. Local agent scratch, not project content. Sits beside .codex/ above for
# the same reason.
.omc/
# Added by code-review-graph
.code-review-graph/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
