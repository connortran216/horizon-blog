# R1.1 local review

User requested removing Vietnamese intro, fixing hero proportions, latest cards, excerpt artifacts and Series, then local FE with production BE.

Plan: compact headline; natural-ratio full images; image-first Latest with aligned metadata; clean summary artifacts; editorial Series with real descriptions. Validate Home tests, types, lint and live local browser. No deployment in this review.

Local-only Vite review config proxies public reads and media resolution to production; blocks other mutations and forwards no credentials. No production API configuration changes.
