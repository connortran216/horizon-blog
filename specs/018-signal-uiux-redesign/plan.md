# Implementation waves

Beads epic: horizon-blog-y2e (authoritative operational store).

1. B1: Foundations
2. B2: Shell and identity
3. B3: Discovery
4. B4: Reading and motion
5. B5: Account and personal pages
6. B6: Author and administration

Serial waves avoid shared theme/shell/card collisions. Critical path: foundation -> shell -> discovery -> reader -> account -> author surfaces. Source graph is current at 34dba9130b72; targeted reads used for concrete file ownership. No backend changes inferred.

G0: B1 ready; downstream plans require refresh against integrated upstream code. No plan is labelled human-approved. G1 requires owned clean worktree and tooling. G2 uses targeted tests/lint/types/build. No commits allowed, so validated B1 remains an uncommitted local handoff; dependent work requires a recorded patch integration base or explicit commit authorization.
