# Signal UIUX migration

Goal: migrate the approved complete prototype to the existing frontend without changing APIs or architecture.

Source: [design handoff](design-handoff/README.md). Each ticket has its own spec and plan.

Scope: all current product routes, shared tokens/components, paired themes and accessible motion. Screen map/UI Kit are prototype review tools, not new public product routes.

Clarification: no material open requirement for B1; user already approved design and requested implementation. No additional production dependencies, env files, commits or external delivery.
